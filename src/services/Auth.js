import axios from "axios";

const LOGIN_URL = "/api/login";
const REG_URL = "/api/register";

const Login = async (user, password) => {
  // fetch
  // store token
  // get data
  return { token: "" };
};

const Register = (user, email, password, confirm_password) => {
  // fetch
  // store token
  // get data
  return { token: "" };
};
